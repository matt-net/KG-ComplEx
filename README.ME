# Project Name

This project implements a training and evaluation pipeline for entity-relation embedding models. The model is designed to train on a dataset of triples and corresponding labels and can be customized with different hyperparameters.

## Setup

### Requirements

Make sure you have Python installed. To install the required dependencies, run:

```bash
pip install -r requirements.txt
